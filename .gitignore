node_modules/
dist/
dist-mobile/
.env
.env.*
!.env.example
.playwright-mcp/
artifacts/
.DS_Store

# Live catalog snapshots and mutable runtime state stay local. The deterministic
# seed catalog lives in server/seed-catalog.ts and is created on first run.
data/*.json
data/*.lock
data/*.lease
data/*.tmp
*.log

# Native projects keep source/configuration in Git; generated sync and build output stays local.
android/.gradle/
android/build/
android/app/build/
android/app/src/main/assets/public/
ios/App/Pods/
ios/App/DerivedData/
ios/App/build/
